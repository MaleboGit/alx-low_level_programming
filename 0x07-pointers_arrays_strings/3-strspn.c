#include "main.h"

/**
 * _strspn - length of a prefix substring
 *
 * @s: pointer parameter to a char
 * @accept: pointer to get bytes from
 *
 * Return: strspn(s, accept
 */
unsigned int _strspn(char *s, char *accept)
{
	return (strspn(s, accept));
}
