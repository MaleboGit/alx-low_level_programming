#include "main.h"

/**
 * _strchr - locates a character in a string.
 *
 * @s: pointer parameter to a character
 * @c: character parameter
 *
 * Return: strchr(s, c)
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
