#include "main.h"

/**
 * _strcmp - compares two strings
 *
 * @s1: character parameter to compare
 * @s2: second char parameter to compare
 *
 * Return: strcmp(s1, s2)
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
