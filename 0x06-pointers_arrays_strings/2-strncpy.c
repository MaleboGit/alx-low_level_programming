#include "main.h"

/**
 * _strncpy - copies a string
 *
 * @dest: character parameter to copy src string
 * @src: string parameter to copy
 * @n: integer parameter
 *
 * Return: strncpy(dest, src, n)
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
