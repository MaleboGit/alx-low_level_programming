#include "main.h"

/**
 * _isupper - checks for uppercase character
 *
 * @c: Integer parameter
 *
 * Return: 0 or 1
 */
int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
