#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: Pointer to a string lateral
 *
 * Return: length
 */
int _strlen(char *s)
{
	size_t length = strlen(s);

	return (length);
}
